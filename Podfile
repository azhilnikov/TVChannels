# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TVChannels' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TVChannels
  pod 'Gloss', '~> 1.2'
  pod 'Kingfisher', '~> 3.0'

  target 'TVChannelsTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Gloss', '~> 1.2'
    pod 'Kingfisher', '~> 3.0'
  end

end
